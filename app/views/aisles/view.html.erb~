<h1>Safeway Storefront</h2>

<h2>Aisle: <%= @aisle.name %></h2>

<%= link_to 'Back', aisles_path %>

<table>
  <tr>
    <th>Product</th>
    <th>Price</th>
  </tr>
 
  <% @products.each do |product| %>
    <tr>
      <td><%= product.title %></td>
      <td><%= product.price %></td>
    </tr>
  <% end %>
</table>
