<h1><%= @aisle.name %> Products</h1>
 
<%= link_to 'Back', (allaisles) %>

<table>
  <tr>
    <th>Product</th>
    <th>Price</th>
  </tr>
 
  <% @products.each do |product| %>
    <tr>
      <td><%= product.title %></td>
      <td><%= product.price %></td>
    </tr>
  <% end %>
</table>
